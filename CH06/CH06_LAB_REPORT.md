# Lab 06: 2436 Ch06

## Student Information
- **Name:** [Uziel Beltran]
- **Date:** [03/08//2026]

## Key Concepts
[BFS, which stands for Breadth-First Search is used to searcg nodes in a graph bu using a queue. It makes sure that the first layer of nodes is fully explored before moving onto the second.]

## What I Learned
[I learned how BFS can be utilized to find the shortest path in an unweighted graph. This was demonstrated through level-order traversal using a queue.]

## Challenges
[One of the main challenges was understanding how to implement the queue correctly to explore the nodes in the correct order. I overcame this by breaking down the algorithm steps and following them systematically.]

## Reflection Questions
1. Why does BFS use a queue instead of a stack?
[BFS uses a queue to explore nodes level by level (FIFO order), ensuring the shortest path (by edges) in unweighted graphs.]

2. What's the difference between BFS shortest path and actual shortest distance?
[BFS finds the path with the fewest edges, not necessarily the shortest distance in terms of weight.]
3. When would you use BFS vs DFS?
[Use BFS for finding the shortest path in unweighted graphs, exploring level by level. Use DFS for searching deeper paths or when stack-based exploration is more suitable.]

*Complete this lab report after finishing the coding portion. Your final commit should include both working code and a completed lab report.*
